title: Events List
resolvable: true
navigable: false
contextual: false
host: /boot/python/runtime
entry:
  run: cap:y5n.caps.system.events.list:main
invocation:
  params:
  - key: limit
resources:
  ref: resource:y5n.caps.system.resources.loader:content
  man:
    default:
      path: events/list/man.ydf
